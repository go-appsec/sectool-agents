name: claude-controller Tests - Pull Request

on:
  pull_request:
    paths:
      - 'claude-controller/**'
      - '.github/workflows/claude-controller-tests-workflow.yml'
      - '.github/workflows/claude-controller-tests-pr.yml'

permissions:
  contents: read

jobs:
  call-reusable:
    uses: ./.github/workflows/claude-controller-tests-workflow.yml
